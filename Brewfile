# Dependencies
brew "git"
brew "gh"
brew "mercurial"  # required for gvm
brew "dolt"
brew "rust"  # cargo, for building the steel-enabled helix fork

# Terminal session attach/detach (zsh/zshrc shows ZMX_SESSION in the prompt)
tap "neurosnap/tap"
brew "neurosnap/tap/zmx"

# Knowledge base. basic-memory's default project and the Obsidian vault are the
# same directory — see VAULT in the Makefile and BASIC_MEMORY_HOME in zsh/zshrc.
tap "basicmachines-co/basic-memory"
brew "basicmachines-co/basic-memory/basic-memory"

# Casks
cask "obsidian"
cask "ghostty"
cask "claude-code"
cask "visual-studio-code"  # also provides the `code` CLI

# Fonts
cask "font-roboto-mono-nerd-font"
